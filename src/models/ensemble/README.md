# Ensemble Models

Placeholder sub-directory for ensemble combination strategies.

No implementation files exist yet — only `__init__.py`. The main ensemble logic lives in `src/models/ensemble_model.py`.
