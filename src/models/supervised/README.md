# Supervised Models

Placeholder sub-directory for supervised classification models (Random Forest, XGBoost, neural networks, etc.).

No implementation files exist yet — only `__init__.py`. Individual model logic is currently embedded in `src/models/ensemble_model.py`.
