# Unsupervised Models

Placeholder sub-directory for unsupervised anomaly detection models (Isolation Forest, autoencoders, clustering).

No implementation files exist yet — only `__init__.py`. Autoencoder and Isolation Forest logic is currently embedded in `src/models/ensemble_model.py`.
